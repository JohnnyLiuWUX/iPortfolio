@{
    ViewBag.Title = "Audit";
	List<IPortfolio.Model.AuditModel> modelList = (List<IPortfolio.Model.AuditModel>)ViewBag.AuditList;
}

<ol class="breadcrumb mtFixed">
    <li><a href="~/">Home</a></li>
    <li><a href="@Url.Action("Index","Audit")">Audit</a></li>
    <li class="active">Audit List</li>
</ol>

<div class="container-fluid">
    <div class="panel panel-default">
        <div class="panel-heading">
            <div class="pull-left">
                <span class="glyphicon glyphicon-list"></span>
                <span>Audit List</span>
            </div>
            
            <div class="pull-right">
                <a href="@Url.Action("Index", "Audit")" class="btn btn-sm btn-default">
                    <span class="glyphicon glyphicon-refresh"></span>
                    <span>Refresh</span>
                </a>
                <a href="@Url.Action("Add", "Audit")" class="btn btn-sm btn-success">
                    <span class="glyphicon glyphicon-plus"></span>
                    <span>Add</span>
                </a>
            </div>
            <div class="clearfix"></div>
        </div>

        <div class="panel-body" style="overflow-y: scroll;">
            <table id="table1" class="table table-striped">
                <thead>
					<tr>
						<th>#</th>
						<th>ProjectID</th>
						<th>Status</th>
						<th>DueDate</th>
						<th>Auditor</th>
						<th>CreatedTime</th>
						<th>...</th>
					</tr>
                </thead>
                <tbody>
                    @foreach (var item in modelList)
                    {
                    <tr>
						<td>@item.AuditID</td>
						<td>@item.ProjectID</td>
						<td>@item.Status</td>
						<td>@item.DueDate</td>
						<td>@item.Auditor</td>
						<td>@item.CreatedTime</td>

                        <td>
                            <a href="@Url.Action("Modify","Audit", new { auditID = @item.AuditID })" class="text-warning" title="Modify">
                                <span class="glyphicon glyphicon-pencil"></span>
                            </a>&nbsp;
                            <a href="javascript:void(0)" onclick="DeleteConfirmYes('@Url.Action("Delete","Audit", new { auditID = @item.AuditID })')" class="text-danger" title="Delete">
                                <span class="glyphicon glyphicon-remove"></span>
                            </a>
                        </td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section scripts {

<!-- DataTables -->
<script src="~/Scripts/jquery.dataTables.min.js"></script>
<script src="~/Scripts/dataTables.bootstrap.min.js"></script>
<script>
    $(document).ready(function () {
        $('#table1').DataTable();
    });
</script>

}
